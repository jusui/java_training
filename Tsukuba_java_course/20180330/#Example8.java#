/*
配列
*/
import static java.lang.System.*;

public class Example8 {
    public static void main(String[] args) {
	/*
	int[] scores;
	scores = new int[6]; // 6つの要素を確保
	socres[0] = 1;
	socres[1] = 3;
	socres[2] = 5;
	socres[3] = 6;
	socres[4] = 5;
	socres[5] = 2;
	*/
	
	// 上記のプログラムコードを1行にまとめる
	int[] counts = {1, 3, 5, 6, 5, 2};
	
	// 配列の要素に順番にアクセス
	for ( int i = 0; i < counts.length; i++ ) {
	    out.println("counts[i]の要素: " + counts[i]);
	    for ( int j = 0; j < counts[i]; j++ ) {
	    	out.println("*");
	    }
	    out.println();
	}
    }
}
